export const formulaForColumnOfFour = (boardSize : number)=>{
    return boardSize*boardSize -3*boardSize -1;
}

// export const formulaForColumnOfFour
export const formulaForColumnOfThree = (boardSize : number)=>{
    return boardSize*boardSize -3*boardSize -1;
}